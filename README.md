# standalone-witness

Standalone witness HTTP daemon for the Attesta transparency log
network.

This repository is being initialized from a split of
`cmd/standalone-witness/` in `clearcompass-ai/ledger`. See the
open PR for the full module import.
